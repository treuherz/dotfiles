-- Make nulls visible
\pset null 'Ø'
-- Make boxes nicer
\pset linestyle unicode
-- Always print timings
\timing
-- Automatically format records according to column width
\x auto
-- If I mess up a complicated command in psql, roll the whole thing back
\set ON_ERROR_ROLLBACK interactive
-- Always complete keywords in uppercase
\set COMP_KEYWORD_CASE upper
